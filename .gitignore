node_modules
dist
*.local
.DS_Store
.vite
*.tsbuildinfo
.env
.env.*
!.env.example

# Ancien site compilé (généré par Manus), conservé en local comme référence.
# Inutile de l'alourdir le dépôt : il reste dans l'historique du dépôt d'origine.
legacy-build/
